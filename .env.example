# Captured from your own authenticated Stockbit session. Keep the real values in .env only.
STOCKBIT_FRAME_1=
STOCKBIT_FRAME_2=
STOCKBIT_FRAME_3=

# Enables frame sizes and decoder diagnostics. Secret values are never logged.
DEBUG_WS=false
